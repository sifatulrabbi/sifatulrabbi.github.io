name: Publish to github pages
on:
  push:
    branches: [dev]
  pull_request:
    branches: [dev]

jobs:
  build:
    runs-on: ubuntu-latest

  steps:
    uses: actions/checkout@v2
    - name: run deploy
      run: npm run deploy:test
